# 分享审查建议的范围裁决（2026-09-14）

来源：https://chatgpt.com/share/6aa75ac3-fcf8-83e8-b99b-515d62c44c4e
该对话是针对 main@93b9eda 的审查及 PR 拆分方案，并非已提交的 GitHub PR。Luna 已提取和阅读，主代理已对照真实源码与定稿；原文的工具引用编号不能作为当前仓库验证结果。

## 保持不变

以 docs/REPLICATION-SPEC.md 为施工真相源，以 HappyClaw-定稿.md 为故事与红线依据：R07 渠道抽象、R14 物化调度、R15 四段 Profile、R19 Cookie 认证、R20 RBAC/Owner Gate 保持不变；产品保持 Profile → Workspace → Runtime Session/Scheduled Run。跨境电商只作为演示场景，不新增虚构使用主体、运营数据或验证效果。

不引入容器/工具沙箱、MCP市场、RAG/记忆系统、Multi-Agent、计费、对象级授权或定稿明确排除的安全机制。HTTP工作区权限不等于工具运行隔离，不能修改文案把尚未实现的隔离包装成已完成。

## 本轮实现范围（已完成）

- B01：WS实际字段校验、与HTTP一致的内容长度边界、传输载荷限制、同步/异步回调异常隔离。
- B02：创建会话前Profile归属校验；HTTP/WS/IM执行时拒绝历史非法Profile引用和跨工作区会话。
- B03：IM注册及回复按渠道账号定位；同渠道多账号互不覆盖；账号与渠道不匹配时拒绝。
- B04（部分）：统一管理命令规范，非owner不能绕过Owner Gate；尚未实现的绑定命令也不进入模型执行。持久化audience策略与管理API属于后续闭环。
- Q01（部分）：CI增加现有Web类型检查及生产构建步骤；补相关回归测试。云端CI运行需推送后才能验证，本轮不提交推送。

## 后续任务，不在本轮伪称完成

| 建议 | 处理结论 |
| --- | --- |
| B05/B06/B07：run-now、计划槽、租约取消/退出排空 | 与R14相容；应独立完成幂等、时间边界和恢复回归，不顺带改调度算法 |
| B08：SDK resume | 与会话模型相容；须锁定0.3.250版本验证真实续接，注入executor通过不能代替真实SDK |
| B09/I01：Provider加密及每轮有效配置 | 已确认网页保存key未进入运行时，明文settings未复用vault；应由一个统一解析器覆盖四种入口并处理旧存储 |
| F01/I03：聊天发送/快照/终态、工具事件及重连 | 与既定Web闭环相容；需联合设计生产者、协议和消费者，不新增孤立消息状态 |
| F02：任务运行状态自动跟踪 | 相容；须覆盖卸载、切换和迟到响应 |
| F03/I04：用户/渠道管理与运行态协调 | 产品操作面扩展；先补真实生命周期和权限接口，再做UI，不凭已配置密钥显示Connected |
| F04：JSON草稿与AI生成 | 保留两阶段确认门禁；当前手工JSON不能包装成已完成AI生成 |
| I02：统一Profile/平台前言及显式会话绑定 | 与三层故事相容；避免group误取isolated会话；cwd不等于沙箱 |
| I05：通知送达 | 相容；只有实际出站成功才可标delivered，通知重试不能重跑模型 |
| Q02：静态托管与运行工件 | 部署方案应单独验收；发现Git跟踪data-dbg-tmp/clawtide.db，仅核实路径，未读取内容、删除或推断泄露 |
| 前一轮短屏视觉问题 | 2026-09-14 续修已完成聊天短屏 CSS 修复及浏览器回归；真实手机软键盘仍未验证 |

## 验证与独立审查

本轮使用 Node 24.19.0（ABI 137），工作目录 E:/zzzz/Clawtide。

- 最终版本全量 Vitest：24 个文件、160 项测试通过，退出码 0。
- 后端 tsc --noEmit、Web tsc -b web --noEmit、后端 tsc -p tsconfig.build.json、Vite build web：均退出码 0；最终入口修复后补跑后端类型检查通过。
- scripts/smoke-loop5.mts：临时数据库 API/WS 冒烟通过，退出码 0；显式清空 ANTHROPIC_API_KEY，验证未配置时的真实失败语义，没有真实 SDK 调用。
- 新增 src/test/pr-security-regressions.test.ts：坏 WS 帧和回调错误、同渠道双账号回复、管理命令前导空白、非法 IM 挂载、跨工作区会话、越权/删除 Profile 均有回归。使用模拟执行器和适配器，未向真实 IM 发送消息。
- Luna 完成审查原文与定稿阅读；Sol 完成实现和针对性测试；Astra 独立只读安全审查发现一项前导空白命令绕过，修复并补测后复审无阻断发现。独立审查未自行运行测试，主代理已运行最终全量测试。
- 本轮业务文件 Prettier 检查及 git diff --check 通过。全局 format:changed 仍因既有未跟踪 AGENTS.md 的格式差异失败；保留用户指令文件，未改写。
- 云端 CI、真实机器人收发、真实 SDK/Provider、部署均未验证；未提交、推送或创建 PR。既有前端修改保留；短屏布局已在下述续修中处理。

## 续修：聊天短屏底部可达性

2026-09-14 用户要求继续修复。本轮仅在 web/src/index.css 增加响应式规则，保留全部既有前后端修改。

- 移除移动端聊天列 260px 最小高度，减少重复标题和留白；消息区继续独立滚动，输入区保持文档流内不收缩。
- 高度不超过 500px 时导航与登出合为一行，保留全部入口；极短屏仍有外层滚动兜底。登出失败提示另起一行，防止挤压导航。
- OpenDesign 本地 MCP 实际读取既有 INTEGRATION.md。Gemini 3.8 Flash medium 通过已获授权 CSS 文本给出布局复核；文件读取路线被其命令权限阻止，未修改权限或绕过限制。
- 真实 Edge/Playwright 页面使用合成接口：9 个视口 × 空状态/历史/加载失败，共 27 组通过；覆盖建议填入、发送、重试、会话抽屉与切换，无 pageerror 或横向溢出。
- 320×568 消息区由 42px 增至至少 164px；390×400 输入区底部由 523px 回到 392px。具体高度随连接提示状态变化。
- 独立 Astra 审查发现登出失败使导航缩至约 9px；复现后修复，320×400 五项导航均为约 44.4×48px，复审无阻断发现。
- Web 类型检查、最终 Web 构建、CSS Prettier 和 git diff --check 均退出码 0。纯 CSS 续修未重复后端单元测试。
- 浏览器证据：C:/Users/ertstyuqk/.codex/visualizations/2026/09/13/01a09a38-cf7d-7040-bac9-feca00afeae8/interaction-final.log；同目录 shortscreen-check.mjs、logout-check.mjs 可复核。
- 真实手机软键盘、安全区、真实 Provider/IM 未验证。未提交、推送或部署。
## 2026-09-16 提交范围

本次本地提交仅包含上述 B01/B02/B03、B04 已实现部分、Q01 已实现部分及安全回归。前端改动混有更早未提交内容，继续保留在工作区；本文记录的短屏修复没有包含在本次提交中。其余建议仍按后续任务表处理。本次未推送远端。
